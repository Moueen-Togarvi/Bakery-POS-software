declare global {
  namespace App {
    // interface Error {}
    interface Locals {
      user: string;
    }
    // interface PageData {}
    // interface PageState {}
    // interface Platform {}
  }
}

export { };
